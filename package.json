{
  "name": "troops-and-tactics",
  "version": "1.0.0",
  "description": "A strategy game with troops and tactics",
  "main": "game.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "mkdir -p dist && cp index.html menu.html game.html style.css dist/ && cp -r src images sounds dist/"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "setupFiles": [
      "./tests/setup.js"
    ]
  }
}
